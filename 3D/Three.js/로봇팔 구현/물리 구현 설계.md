

### 하나의 arm 파트 기준

- **물리(world) 등록은 arm 하나**, **시각(local)은 arm + joint 여러 개** 구조.

```
[ Arm RigidBody ]
   ↕
[ Arm Group (Object3D) ]
 ├─ arm mesh
 └─ joint mesh
```

### 다음 arm과는

- 설정 : 충돌 비활성화 `joint.setContactsEnabled(false) `

```
Arm1 RigidBody ──(Rapier Joint)── Arm2 RigidBody
```