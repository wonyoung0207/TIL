

# 웹서버와 WAS 개념정리 

---

## 용어정리 

1. 서버측 프로그램 언어
   - PHP, JSP, Nodejs(Javascript 기반)
2. 서버
   - 아파치, 톰캣, Nodejs

## 웹 서버 ( Web Server )

### 정의

- "HTTP 요청을 받아서 HTML 문서와 같은 웹 페이지를 반환하는 프로그램"
- 클라이언트의 요청에 의해 정적 파일을 제공하는 서버. Apache, NginX, IIS등의 종류가 있으며, 정적파일만 제공할 수 있다.
- ex)
  - 사용자가 브라우져를 통해 네이버에 접속한다고 한다면, 브라우져 주소표시줄에 http://naver.com 라고 치면 이 행위를 요청이라고 한다. 
  - 서버란, 이 사용자의 요청을 기다리고 그 요청에 따른 데이터(HTML, CSS, JS, Image 등)을 전송해주는 프로그램이다. 

## 웹 어플리케이션 서버 (Web Application Server)

### 정의

- DB 조회나 다양한 로직 처리를 요구하는 동적인 컨텐츠를 제공하기 위해 만들어진 Application Server
- Web Server + Web Container 의 개념이다. 
- 웹 서버와 웹 컨테이너를 포함한 개념. 웹 서버에서는 정적 파일만 제공하므로, 동적으로 DB와 연결하여 여러 데이터를 활용해 Php, Jsp, Asp로 표현된 페이지를 해석할 수 있어야 하는데 이 역할을 웹 컨테이너가 제공한다.

### 역할

- WAS는 JSP, Servlet 구동 환경을 제공한다.
  - Container란 JSP, Servlet을 실행시킬 수 있는 소프트웨어를 말한다.
- Web Server 기능들을 구조적으로 분리하여 처리하고자하는 목적을 가진다. 

### 종류

1. Tomcat
2. JBoss
3. Jeus
4. Web Sphere

## 웹 서버와 WAS 분리 이유 

- 두개를 분리하면 웹 서버를 한번 거치기에 보안상의 이점 존재. 

- 또한 웹 서버에서 고장난 WAS로 보내지 않고, 그 시간동안 이를 복구시킨다면 사용자는 끊김없이 서버에서 서비스를 제공받을 수 있다.

  